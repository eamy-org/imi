# Config file for automatic testing at travis-ci.org

language: python
evn:
    global:
        PYTHONUNBUFFERED=yes
before_install:
  - |
    python --version
    uname -a
    lsb_release -a
install:
  - pip install tox
script:
  - tox -v -- -v
